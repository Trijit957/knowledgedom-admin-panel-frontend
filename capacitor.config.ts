import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'knowledgedom-admin-panel-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
